query GueryMessagesByChannelId($id: UUID!) {
  channelById(id: $id) {
    messagesByChannelId(orderBy: UPDATED_AT_ASC) {
      nodes {
        nodeId
        id
        text
        memberId
        createdAt
        updatedAt
      }
    }
  }
}
