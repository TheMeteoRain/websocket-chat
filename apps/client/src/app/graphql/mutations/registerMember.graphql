mutation RegisterMember(
  $firstName: String!
  $lastName: String!
  $email: String!
  $password: String!
) {
  registerMember(
    firstName: $firstName
    lastName: $lastName
    email: $email
    password: $password
  ) {
    id
    firstName
    lastName
  }
}
